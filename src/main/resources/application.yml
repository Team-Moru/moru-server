spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: moru-server
  http:
    clients:
      connect-timeout: 3s
      read-timeout: 5s
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}

server:
  port: ${SERVER_PORT:8080}
