# Add these directives to the http block before the virtual host includes.
log_format moru_privacy
    '$time_iso8601 request_id=$sent_http_x_request_id '
    'status=$status duration=$request_time';

access_log /var/log/nginx/access.log moru_privacy;
